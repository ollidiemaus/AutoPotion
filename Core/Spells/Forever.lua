local addonName, ham = ...
if not ham.isForever then return end

-- Forever-only spells go here, e.g.:
-- ham.someForeverOnlySpell = ham.Spell.new(123456)
-- table.insert(ham.supportedSpells, ham.someForeverOnlySpell)
